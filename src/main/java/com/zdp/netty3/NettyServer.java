package com.zdp.netty3;

//import org.jboss.netty.bootstrap.ServerBootstrap;
//import org.jboss.netty.channel.ChannelPipeline;
//import org.jboss.netty.channel.ChannelPipelineFactory;
//import org.jboss.netty.channel.Channels;
//import org.jboss.netty.channel.socket.nio.NioServerSocketChannelFactory;

/**
 * netty 3.* 版本的用法<br/>
 * Created by dapeng.zhou on 2016/6/14.
 */
public class NettyServer {
    public static void main(String[] args) {
//        System.out.println(Integer.valueOf("0M", 36));
//        Executor bossExecutor = Executors.newCachedThreadPool();
//        Executor workerExecutor = Executors.newCachedThreadPool();
//        ServerBootstrap sb = new ServerBootstrap(new NioServerSocketChannelFactory(bossExecutor, workerExecutor));
//        sb.setPipelineFactory(new ChannelPipelineFactory() {
//            public ChannelPipeline getPipeline() throws Exception {
//                return Channels.pipeline(new DemoServerChannelHandler());
//            }
//        });
//        System.out.println("server starting...");
//        sb.bind(new InetSocketAddress(9090));
    }
}
